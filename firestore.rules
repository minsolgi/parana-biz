rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 'books' 컬렉션의 모든 문서에 대해
    match /books/{bookId} {
      // 로그인한 사용자만 문서를 읽고 쓸 수 있도록 허용
      allow read, write: if request.auth != null;
    }
  }
}